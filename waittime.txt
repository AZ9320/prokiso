#とりあえず考えてること

#1.階数(floor)入力
#2.定員(capa)入力
#3.1階あたりの移動時間(ele_time)←止まらない回が途中にあったら、の場合は後で考える
#4.各階の待ち人数(p[i])
#を取得。
#乗り降りの時間をp≠0ならば15+p[i]、p=0ならば0とする


import math
import numpy as np

floor=int(input("階数を入力してください："))#階数入力

i=1
while i<=floor:
    