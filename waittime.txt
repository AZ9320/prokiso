#とりあえず考えてること

#1.階数(floor)入力
#2.定員(capa)入力
#3.1階あたりの移動時間(ele_time)←止まらない回が途中にあったら、の場合は後で考える
#4.各階の待ち人数(p[i])
#を取得。
#乗り降りの時間をp≠0ならば15+p[i]、p=0ならば0とする


people=[5,3,1,4,0]

n=int(input("何階？"))
if n<len(people):
    print(sum(people[:n-1])+15*(n-1))
